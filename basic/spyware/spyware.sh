#!/usr/bin/env bash
source $(dirname $0)/../../current_directory.sh

printf "Spyware is located at $(directory)!!\n"
echo "Type 'autosshfs' to mount a network drive with sftp!!"
echo "Type 'networkconfig' to set a static ip address!!"
echo "Type 'sshcopy' to copy ssh keys to remote computer!!"
echo "Type 'mountlocal' to mount a local hard disk!!"
echo "Type 'shortcut' to create a shortcut to a command!!"

printf "Spyware is located at $(directory).."
